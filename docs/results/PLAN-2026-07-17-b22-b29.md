# PLAN-2026-07-17 · B22–B29 真机缺口

> 基线 APK：`4902430` / sha256 `0817dadb…`（B14–B21 READY）  
> 主线程：方案 / 调度 / 验收 · **禁止写业务码**  
> 用户纠偏：**B26 不是去 max/ultra**；按 `model/list` 每模型 `supportedReasoningEfforts` 动态展示（sol→max+ultra；其它 5.6→max）

## 映射

| ID | 用户 | 结论摘要 |
|----|------|----------|
| **B22** | @ 再按不收列表 | 按钮 always-open；二按应 toggle → hide |
| **B23** | # 同理 | `_triggerSlashCommandPanel` always-open；二按 hide + 清裸 `/` |
| **B24** | 目标模式无反应/不回话 | enable 后 `goal:null` 误杀 mode；OFF==clear；mode ON 全走 setGoal |
| **B25** | 默认权限无弹窗/无执行权 | `writableRoots:[]` 覆盖原生 cwd 默认；thread/start 沙箱字段错 |
| **B26** | 模型/思考硬编码 | 取消全局禁 max/ultra；按模型 catalog；本地 modelId→efforts 图 |
| **B27** | 找不到上下文/压缩阈值 | 聊天环隐藏且仅长按；Codex 缺 `auto_compaction` 开关 |
| **B28** | 日志有无落盘 | **已落盘**（见 §B28）；本轮只保证可感知 + 验收文案 |
| **B29** | 做完/停下要通知栏 | complete 有；abort/error 缺 notify |

## 锁

| Lock | 文件 | Owner |
|------|------|-------|
| **L-Panel** | `chat_page_ui.dart` wire；`chat_page_openclaw.dart` trigger/hide | B22+B23 |
| **L-CodexCore** | `chat_page_codex.dart`（goal / perm / effort 分段） | **单代理串行** B25→B24→B26 |
| **L-Kotlin-Perm** | `CodexAppServerManager.kt` startThread sandbox 字段 | B25 |
| **L-Service** | `codex_app_server_service.dart` 若 startThread 扩参 | B25 可选 |
| **L-Context** | `conversation_model.dart` ratio；composer ring；`codex_setting_page` auto_compaction | B27 |
| **L-Notify** | `CodexAppServerManager` 终态；`AssistsCoreManager` onError/cancel | B29 |
| **L-Composer-Effort** | `chat_input_area_composer` 默认 efforts / labels | B26 辅 |

## 完成标准

1. **B22/B23** 开列表再按同按钮 → 收起；键盘可保留  
2. **B24** 空线程开目标模式保持 ON；设目标后有 turn；X 清目标；跨会话不误杀  
3. **B25** default：`writableRoots` 非空 + on-request；需授权时出审批卡；full 仍可用  
4. **B26** `gpt-5.6-sol` 显示 max+ultra；其它 5.6 显示 max；无硬编码全局禁  
5. **B27** 普通模式上下文环可发现并改阈值；Codex 设置有 auto_compaction  
6. **B28** DELIVERY 写明路径；debug 包可见 log 文件  
7. **B29** turn 完成/用户停/失败 → 通知栏（设置开且非当前前台同会话可抑）  
8. GHA Baseline Standard Debug success + APK stage + DELIVERY

## 非目标

- 不 push origin / omnimind-ai  
- 本机不 Gradle 出包  
- 不改 B14 Fast 三写主路径（除非回归）

## Wave

- **W0** scout ✅  
- **W1** 6 路并发实现（L-CodexCore 单线程内 B25→B24→B26）  
- **W2** 静态验收 + commit + push mine + GHA + stage  
- **W3** DELIVERY + 用户测点  

## B28 日志结论（给用户）

| 项 | 值 |
|----|-----|
| 主路径 | `/storage/emulated/0/Download/OmniBotLogs/omnibot-debug-YYYYMMDD.log` |
| 条件 | **debug 包**（`kDebugMode`）且未 force-disable |
| 现状 | 设备上 **已有** `omnibot-debug-20260716.log` / `20260717.log`；含 `turn_start` / `goal` / `permission_set` |
| 回退 | app external files `…/OmniBotLogs/` |

---

真源：本文件 · 执行见 `EXEC-2026-07-17-b22-b29.md`
