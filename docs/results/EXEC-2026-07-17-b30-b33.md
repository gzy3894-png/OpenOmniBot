# EXEC-2026-07-17 · B30–B33

真源：`PLAN-2026-07-17-b30-context-panel-model-perm.md`  
基线：`cb7ce48` / READY `7793303`  
主线程：方案/调度/验收；子代理实现；不本机 Gradle；push 仅 `mine`；GHA `baseline-standard-debug`。

## 锁与并发

| Lock | 覆盖 | 独占文件 | 并行 |
|------|------|----------|------|
| **L-CodexCore** | B31 skills open + B32 model catalog + B33 auto-compact handler | `chat_page_codex.dart` | 串行整文件 1 agent |
| **L-PanelOpen** | B31 `/` 开清 skills；route 互斥 | `chat_page_openclaw.dart`, `chat_page.dart` (route/flags only) | ‖ |
| **L-Composer** | B33 隐藏权限按钮；B32 菜单不 inject current | `chat_input_area_composer.dart` | ‖ |
| **L-ConfAPI** | B30 阈值 conf 读写 | `codex_app_server_service.dart`, `CodexAppServerManager.kt` | ‖ |
| **L-ContextBar** | B30 新半透明顶栏组件 | **new** `codex_context_bar.dart` | ‖ |
| **L-ChatUI** | B30 topBanner；B31 @ wire；B33 null perm + slash 卡 | `chat_page_ui.dart` | ‖ 与 L-CodexCore 并行（不同文件） |

≥6 并发：上表 6 锁一次拉起。

## 完成标准（静态）

1. **B30**：Codex topBanner 含上下文条；保存写 conf（`omnimind_context_token_threshold` + 可选 auto_compaction）；toast 含「新开对话」；底栏环仍仅 normal。
2. **B31**：开 `/` 清 `_codexSkillsPanelVisible`；开 `@` 不污染 root 路由；slash 前缀优先 root。
3. **B32**：`_codexModelOptions` = list 提取结果；无 merge 注入 list 外 id；composer 菜单同。
4. **B33**：composer 无权限按钮；`/` 有 goal/fast/auto-compact；fast 默认关；payload 固定 defaultMode。

## 非目标

- 不回滚 B14 Fast / B25 roots / B26 effort catalog
- 不本机 assemble；不 push origin

## 波次

- W1：6 锁并行实现 + 各写 `docs/results/reports/impl-b3x-*.md`
- W2：主线程静态验收 + 单次 feature commit + push mine + GHA + stage APK + DELIVERY
