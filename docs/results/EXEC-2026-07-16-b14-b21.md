# EXEC · B14–B21 · 2026-07-16

> 主线程：方案/调度/验收 · 子代理实现 · push 仅 mine · GHA baseline-standard-debug
> 真源方案：`PLAN-2026-07-16-next-fast-perm-slash.md`

## 波次

| Wave | 内容 | 并发 | 锁 |
|------|------|------|----|
| 0 | 只读 scout 8 模块 | 8 | 无写 |
| 1 | B14 → B20 → B19 | 串行合入优先；scout 后可并行改不同文件 | L-Fast / L-Perm / L-Model |
| 2 | B15 B18 B17 B16 + B21 补齐 | 可并行 | L-Slash / L-Compact / L-Layout / L-Log |
| 3 | 静态门禁 + push mine + GHA + stage APK + DELIVERY | 1 | ship |

## 文件锁（实现时必须遵守）

| 锁 | 主文件 | Bugs |
|----|--------|------|
| L-Fast | `ui/.../chat_page_codex.dart`（fast 段）+ `CodexAppServerManager.kt`（normalize/toml）+ `codex_setting_page.dart` | B14 |
| L-Perm | `chat_page_codex.dart`（permission/approval 段）+ service startTurn params | B20 |
| L-Model | `chat_page_codex.dart` effort 归一 + UI options 源 | B19 |
| L-Slash | `chat_page_ui.dart` 白名单 + slash hide/focus 路径 | B15 B18 |
| L-Compact | compact tip + DebugFileLog | B17 |
| L-Layout | `chat_input_area_composer.dart` @ 色 | B16 |
| L-Log | `debug_file_log.dart` + 各锁埋点 | B21 |

**注意**：`chat_page_codex.dart` 是多锁热点 → Wave1 建议 **单代理串行改 B14+B20+B19 核心**，或按函数段落分段合入，避免冲突。Wave2 可并行非重叠文件。

## 完成定义

- 关 Fast：三写（settings RPC + config `fast_mode=false` + turn 不吃默认 fast）+ fast_set 日志
- 权限：RPC + requestApproval 弹窗可批
- effort：仅 supported 可选
- slash：无 stop/skills；点后收列表保键盘
- compact：可感知 + 日志
- @ 同色
- GHA success + Download APK + DELIVERY 测点 B14–B21

## 非目标

- 不 push origin/omnimind-ai
- 本机不 Gradle
- 主线程不写业务码
